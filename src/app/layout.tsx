import type { Metadata } from "next";
import { Fira_Code, Inter } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";
import { Button } from "@/components/ui/button";
import { ScreenIndicator } from "@/components/SignalIndicator";
const FiraCode = Fira_Code({ subsets: ["latin"] });
const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Franz",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={cn("bg-gray-200", inter.className)}>
        <div className="flex">
          <aside className="items- flex h-[100vh] w-[20vw] flex-col justify-between gap-y-8 bg-gray-200 px-5 py-5 text-white">
            <div
              className={cn(
                FiraCode.className,
                "mt-5 text-[18px] font-extrabold text-black",
              )}
            >
              _franz
            </div>
            <div className={(cn(inter.className), "font-bold text-black")}>
              <ul className="flex flex-col gap-y-7">
                <li className="cursor-pointer">Home</li>
                <li className="cursor-pointer">About Me</li>
                <li className="cursor-pointer">Projects</li>
                <li className="cursor-pointer">Contact</li>
              </ul>
            </div>
            <div>
              <Button
                variant={"outline"}
                className="flex w-full items-center text-black"
              >
                Light
              </Button>
            </div>
          </aside>
          <main className="mx-5 mt-5 w-full rounded-t-[30px] bg-white">
            <div className="overflow-auto p-5">{children}</div>
          </main>
          {process.env.NODE_ENV !== "production" && <ScreenIndicator />}
        </div>
      </body>
    </html>
  );
}
